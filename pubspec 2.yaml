name: teenpay
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0

  
  # --- CRITICAL FIX 1: provider MUST align with other dependencies ---
  provider: ^6.0.5 
  image_picker: ^1.2.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # --- CRITICAL FIX 2: assets MUST align with uses-material-design ---
  assets:
    - assets/login-icons/
    - assets/KYC-icons/